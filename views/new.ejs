<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Upload Image</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: linear-gradient(135deg, #f9ce34, #ee2a7b, #6228d7);
      color: #fff;
      text-align: center;
      padding: 50px;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
    }

    h1 {
      font-size: 2em;
      margin-bottom: 20px;
      text-shadow: 1px 1px 4px rgba(0, 0, 0, 0.3);
    }

    form {
      background-color: rgba(255, 255, 255, 0.1);
      padding: 20px 30px;
      border-radius: 15px;
      backdrop-filter: blur(10px);
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
      width: 90%;
      max-width: 400px;
    }

    label {
      font-size: 1.1em;
      font-weight: bold;
      display: block;
      margin-top: 10px;
    }

    input[type="text"],
    textarea {
      margin-top: 10px;
      width: 100%;
      padding: 10px;
      border: none;
      border-radius: 8px;
      font-size: 1em;
      outline: none;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      resize: none;
    }

    input[type="submit"] {
      margin-top: 20px;
      padding: 12px 20px;
      border: none;
      border-radius: 25px;
      font-size: 1em;
      font-weight: bold;
      background: #fff;
      color: #ee2a7b;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    input[type="submit"]:hover {
      background: #ee2a7b;
      color: #fff;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
    }
  </style>
</head>

<body>
  <h1>Upload Your Image to Post on Instagram</h1>
  <form action="/cars" method="post">
    <label for="image_url">Enter Image URL:</label>
    <input type="text" id="image_url" name="source" placeholder="https://example.com/image.jpg">

    <label for="caption">Enter Caption:</label>
    <textarea id="caption" name="caption" rows="3" placeholder="Write your caption here..."></textarea>

    <input type="submit" value="Submit Post">
  </form>
</body>

</html>